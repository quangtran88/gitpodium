---
name: Bug report
about: Something broke or produced wrong numbers
---

**What happened / what did you expect?**

**The exact command that failed** (and its output):

```
```

**Environment**
- OS:
- `git --version`:
- `gh --version`:
- `python3 --version`:
- `gitpodium version` (or `git rev-parse --short HEAD` in the install dir):
